// Type Every Variable and Function
// Explicit typing for variables
let count: number = 5;

// Explicit typing for function parameters and return type
function addNumbers(a: number, b: number): number {
    return a + b;
}